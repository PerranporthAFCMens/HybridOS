name: HybridOne Auth secret presence check

on:
  push:
    branches: [dev]
    paths:
      - '.github/workflows/auth-secret-check.yml'

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check Supabase Auth mail secrets
        shell: bash
        run: |
          set -euo pipefail
          curl -fsS 'https://mzgnhmeydhhpzgxlgudh.supabase.co/functions/v1/hybridone-auth-secret-check?token=diag_9f8d2a7c31b64e48a0b5c2d1e7f943ab'
