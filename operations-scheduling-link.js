(function(){if(!location.pathname.endsWith('/admin-operations.html'))return;function init(){const pane=document.getElementById('resources');if(!pane||document.getElementById('resourceAvailabilityBtn'))return;const toolbar=pane.querySelector('.toolbar');if(!toolbar)return;const btn=document.createElement('a');btn.id='resourceAvailabilityBtn';btn.className='btn secondary';btn.href='./resource-availability.html';btn.textContent='🕒 Resource availability';btn.style.textDecoration='none';const add=toolbar.querySelector('#addResourceBtn');if(add)add.before(btn);else toolbar.appendChild(btn)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init()})();